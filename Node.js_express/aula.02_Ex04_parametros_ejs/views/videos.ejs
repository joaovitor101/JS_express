<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Videos</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        h1 {
            color: #4a90e2;
            margin-bottom: 20px;
        }

        .container {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: 80%;
            max-width: 600px;
            text-align: center;
        }

        hr {
            border: 0;
            border-top: 1px solid #ddd;
            margin: 20px 0;
        }

        p {
            margin: 10px 0;
        }

        a {
            text-decoration: none;
            color: #4a90e2;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
            color: #357abd;
        }

        .no-content {
            color: #888;
        }
    </style>
</head>
<body>
    <div class="container">
        <% if (!playlist) { %>
            <h1>Bem-vindo à página de vídeos</h1>
            <hr>
            <p>Escolha uma playlist:</p>
            <div>
                <% listaPlaylist.forEach((playlist) => { %>
                    <p><a href="/videos/<%= playlist %>"><%= playlist %></a></p>
                <% }) %>
            </div>
        <% } else if (!video) { %>
            <h1>Playlist <%= playlist %></h1>
            <hr>
            <p>Escolha um vídeo:</p>
            <div>
                <% listaVideo.forEach((video) => { %>
                    <p><a href="/videos/<%= playlist %>/<%= video %>"><%= video %></a></p>
                <% }) %>
            </div>
        <% } else { %>
            <h1>Playlist <%= playlist %></h1>
            <hr>
            <p>Tocando o vídeo <%= video %>...</p>
        <% } %>
    </div>
</body>
</html>
